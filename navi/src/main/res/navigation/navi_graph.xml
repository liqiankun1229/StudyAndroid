<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/navi_graph"
    app:startDestination="@id/first">
    <fragment
        android:id="@+id/first"
        android:name="com.lqk.navi.fr.AFragment"
        android:label="first">
        <action
            android:id="@+id/f_click"
            app:destination="@id/b" />
    </fragment>

    <fragment
        android:id="@+id/b"
        android:name="com.lqk.navi.fr.BFragment"
        android:label="b">
        <!-- 参数 -->
        <argument
            android:name="UserName"
            android:defaultValue=""
            app:argType="string"
            app:nullable="true" />
    </fragment>
</navigation>